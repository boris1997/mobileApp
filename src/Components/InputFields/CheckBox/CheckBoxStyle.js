import { StyleSheet } from "react-native";

export const CheckBoxStyle = StyleSheet.create({
    boxView: {
        flexDirection: 'row', 
        marginLeft: 10
    },

    text: {
        marginLeft: 5
    }
});